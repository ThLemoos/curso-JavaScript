// Tipos de Dados Primitivos

// Strings
const nome = 'Thiago'; // Aspas simples
const nome1 = "Lemos"; // Aspas duplas
const nome2 = `Ferreira`; // Entre crases

// Number
const num1 = 10; // Número inteiro
const num2 = 10.52; //Número real ou de ponto flutuante

// Undefined
let nomeAluno; // Quando declaramos uma variável sem incializá-la
console.log(typeof nomeAluno, nomeAluno);

// Null
let corDeFundo;
corDeFundo1 = null; // Se caso em um site que criarmos o usuário não escolher cor nenhuma
console.log(corDeFundo);

corDeFundo = 'red'; // Se caso em um site que criarmos o usuário escolher a cor vermelha
console.log(corDeFundo);

// Boolean (verdadeiro ou falso)

let aprovado;

aprovado = true; // Se caso o aluno for aprovado
console.log(aprovado)

aprovado = false; // Se caso o aluno não for aprovado
console.log(aprovado);

// Typeof(): serve para ver o tipo e o valor da variável em questão

console.log(typeof num1); // Mostra o tipo da variável
console.log(typeof num1, num1); // Mostra o tipo e o valor da variável
